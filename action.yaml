name: open PR
author: Wanderlucio P Silva
description: This action open a pull request for a specified branch

inputs:
  branch:
    description: the branch which will receive de PR
    required: true

runs:
  using: composite
  steps:
    - run: gh --version
      shell: bash
    - run: gh pr create --head teste --base main --fill 
      shell: bash


branding:
  icon: 'cpu'
  color: 'green'